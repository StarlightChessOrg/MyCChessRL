# 运行时（与 pyproject.toml 对齐；pip install -r requirements.txt）
numpy>=1.24
torch>=2.1
# 监督学习 train_sl：.cbf 解析与 icyElephant/MyElephant 一致（xmltodict）
xmltodict>=0.13

# 网页对弈
sanic>=23.12

# C++ 扩展 xqwl_core 需本地 CMake 编译安装，无 pip 包名
